# フロントエンド連携メモ

### フロントに返す JSON データ

```python
# カテゴリー追加機能
"category_id": category.category_id,
"category_title": category.title,
"user_id": category.user_id,

# カテゴリー名の編集機能
"category_id": category.category_id,
"category_title": category.title,
"user_id": category.user_id,

# カテゴリーの並べ替え（ソート）機能
```
